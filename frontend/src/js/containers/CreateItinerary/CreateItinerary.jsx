import React from 'react';
import PropTypes from 'prop-types';
import { connect } from 'react-redux';

class CreateItinerary extends React.Component {
    state = {
        step: 1,
    }

    render() {
        <CreateUpdateItinerary />
    }

};

export default connect()(CreateItinerary);